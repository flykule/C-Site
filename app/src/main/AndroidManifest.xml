<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.castle.csite">

    <application
        android:name=".util.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/default_share_image"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.SettingFragment"/>
        <activity android:name=".ui.activity.MainActivity"/>
        <activity android:name=".ui.activity.InterestActivity"/>
        <activity android:name=".ui.activity.OffLineCacheActivity"/>
        <activity android:name=".ui.activity.TabActivity"/>
        <activity android:name=".ui.activity.LoginActivity"/>
        <activity android:name=".ui.activity.RegistActivity"/>
        <activity android:name=".ui.activity.MyThemeActivity"/>
        <activity android:name=".ui.activity.MyWalletActivity"/>

    </application>

</manifest>